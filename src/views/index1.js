import MaiaChat from './src/index';

//Messages
import {
    addUserMessage,
    addResponseMessage,
} from './src/store/dispatcher';

export {
    MaiaChat,
    addUserMessage,
    addResponseMessage,
}